// Direttiva per il pre-processore (stdio.h => standard I/O library)
#include <stdio.h>

// Funzione main non deve mai mancare e deve restituire un integer
int main() {
	printf("Hello World\n");
	
	return 0; // 0 => tutto OK (usato come standard)
}